export const DEFAULT_THRESHOLD = 2;
export const DEFAULT_LIMIT = 5;
// A species the group has only ever recorded in this many periods or fewer is
// rare enough to be worth a line every time it turns up again.
export const DEFAULT_MAX_APPEARANCES = 3;
